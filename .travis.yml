# travis CI for testing TeX package cu-num
os: linux

addons:
  apt:
    packages:
    - texlive-binaries
    - texlive-xetex

install:

script:
    - cd cu-num
    - xelatex --interaction=nonstopmode cu-num-test.tex
    - cd cu-calendar
    - xelatex --interaction=nonstopmode cu-calendar-test.tex
