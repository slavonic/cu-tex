# travis CI for testing TeX package cu-num
os: linux

addons:
  apt:
    packages:
    - texlive-binaries
    - texlive-xetex
    - texlive-latex-extra

install:

script:
    - cd cu-num
    - xelatex --interaction=nonstopmode cu-num-test.tex
